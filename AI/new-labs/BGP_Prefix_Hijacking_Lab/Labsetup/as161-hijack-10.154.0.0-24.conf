# Add this block to AS-161's BIRD configuration to announce AS-154's /24.
# This simulates an equal-prefix hijack.

protocol static {
    ipv4 {
        table t_bgp;
    };
    route 10.154.0.0/24 blackhole {
        bgp_large_community.add(LOCAL_COMM);
    };
}
