version: "3"

services:
   server:
      build: 
         context: ./src/server
         args: 
            BUFFER_SIZE: 100
      container_name: ctf_fmt_str_srvr
      tty: true
      ports:
         - "7777:7777"

