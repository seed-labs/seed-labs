#!/bin/bash

cat << EOF

Reminder: We need to do the following manually:


** Firefox: Install HTTP Header Live and configure it

** OS: Disable the system upgrade reminder (see OS configuration in document)

** OS: Fix Ubuntu internal error problem (see OS configuration in document)

EOF

