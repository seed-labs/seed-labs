# Notes regarding the Ubuntu OS


- The `docker-compose` command has been integrated into the `docker` command,
  so we should use `docker compose`. We need to change the aliases that 
  we created for the docker commands. 



